----- How To ----

Install:
	1.) source setupTiming.sh
	2.) make
	3.) mkdir -p $HOME/local/bin
	3.) make install (moves to ~/local/bin)
	4.) Timing --help

Install adds Timing.sh, which sets up environment:
Run (with setup):
    	1.) Add ~/local/bin to path
	2.) source setupTiming.sh
	3.) Timing --help

Run (without setup):
    	1.) Add ~/local/bin to path
	2.) Timing.sh --help
